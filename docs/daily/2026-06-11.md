---
title: Дневная заметка - 2026-06-11
date: 2026-06-11
status: active
doc_type: daily
area: frontend
task_tokens:
  - HRB-P1-07
  - HRB-P1-03
  - HRB-P2-05
source_of_truth: false
---

# Дневная заметка - 2026-06-11

- Stage обновлен до `03eeb7f`; web на `/login` и React operator surfaces отвечает штатно через auth redirect.
- Stage worker переведен на правильный Telegram bot token `@ze_hr_bot`; старый webhook снят, long polling снова управляется нашим `hr-bot-worker`.
- Во время ручного тестирования зафиксирован UX-gap по bot menu: изменение menu sets в админке не обновляет уже показанную пользователю reply keyboard само по себе.
- Текущее наблюдаемое поведение по bot menu:
  - admin меняет кнопки в `/app/bot-menu`;
  - Telegram-клиент пользователя продолжает показывать последнюю присланную клавиатуру;
  - новое меню появляется только после нового `send_menu(...)`, например через `/start` или другой bot-driven resend.
- Зафиксирован ожидаемый follow-up:
  - меню бота должно уметь обновляться из админки явным действием оператора;
  - предпочтительный MVP-вариант: admin-side кнопка `Переотправить меню сотруднику`, а не требование к сотруднику повторно нажимать `/start`.
- Backend-contract для `/app/bot-menu` уточнен:
  - `available_scenarios` теперь отдает только сущности `scenario`, без `survey`;
  - `employee_options` теперь содержит metadata для фильтрации по аудитории и должности: `employee_stage`, `candidate_work_stage`, `desired_position`, `role_scope`;
  - backend валидация кнопок меню теперь отклоняет попытку привязать к `launch_scenario` опрос вместо сценария.
- Legacy form routes для сохранения кнопок меню подтянуты к той же валидации, что и React API, чтобы старый экран не мог записывать неконсистентные ссылки на menu set/document/scenario.
- Во время разбора карточки сотрудника не найден явный autosave по `onChange`: React detail screen сохраняет данные через submit `POST /api/employees/{id}`.
  - Если поле визуально "сохранилось само", это надо воспроизвести отдельно: вероятнее всего, сработал другой submit-path или повторная загрузка payload, а не намеренный autosave.
